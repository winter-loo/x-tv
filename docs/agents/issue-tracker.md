# Issue tracker

Work is tracked in GitHub Issues at https://github.com/winter-loo/x-tv.
Use `gh issue view <number> --repo winter-loo/x-tv --comments` to read the task.
Implementation-ready tickets use `ready-for-agent`; start only when their native GitHub blocking dependencies are closed.

PRs as a triage surface: no.
